# Deny direct access to the database file
<FilesMatch "\.db$">
    Require all denied
</FilesMatch>

# Deny access to dotfiles (except .htaccess itself)
<FilesMatch "^\.(?!htaccess)">
    Require all denied
</FilesMatch>
